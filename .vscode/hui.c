#include<stdio.h>
#include"bst.h"
int main()
{
    struct node *root=NULL;
    for (int i = 0; i < n; i++)
    {
        root=insert(root,5);
    }

    
    
    printf("Hello world!\n");
    return 0;
}